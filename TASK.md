Now inspect ONLY /static/js/admin.js.

Find all fetch() calls and list the API endpoints they call.

Do NOT modify anything.
Do NOT restart the server.
Do NOT inspect other files yet.
Do NOT commit or push.

Most importantly, identify which endpoint is causing the Admin page "Failed to fetch".